[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "spaceship_project"
version = "0.1.0"
description = "Spacship Titanic ML Pipeline"
authors = [
    {name = "Bevlin"}
]

[tool.setuptools.packages.find]
where = ["."]